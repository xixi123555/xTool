# xTool MCP Server 环境变量示例
# 复制为 .env 并填入实际值

# xTool Server 地址（默认 localhost:5198）
XTOOL_SERVER_URL=http://localhost:5198

# JWT Token（从 xTool 应用登录后获取，用于记账、待办等需认证的接口）
XTOOL_JWT_TOKEN=

# Dify API Key（可选，网页阅读器专用。若不配置且已登录，将尝试从 appkey 获取 web_reader）
DIFY_API_KEY=
